info:
  name: Health check post
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/health
  body:
    type: json
    data: |-
      {
        "status": "servidor funcionando"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
